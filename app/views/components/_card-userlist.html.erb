<div class="container-carousel">
 <% @lists.each do |list| %>
 <%= link_to list_path(list), class:'card-label text-decoration-none' do %>
  <div class="card">
    <div class="card-label">
        <%= list.name %>
        <% end %>
        </div>
    </div>
    <% end %>
</div>